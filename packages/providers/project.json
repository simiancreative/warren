{
  "name": "providers",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/providers/src",
  "projectType": "library",
  "tags": [],

  "// targets": "to see all targets run: nx show project providers --web",
  "targets": {
    "build": {
      "options": {
        "cwd": "packages/providers"
      }
    },

    "release": {
      "options": {
        "cwd": "packages/providers"
      }
    },

    "publish": {
      "options": {
        "cwd": "packages/providers/dist"
      }
    },

    "lint": {
      "options": {
        "cwd": "packages/providers"
      }
    },

    "typecheck": {
      "options": {
        "cwd": "packages/providers"
      }
    },

    "test": {
      "options": {
        "cwd": "packages/providers"
      }
    }
  }
}
